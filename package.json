{
  "name": "tafys-actor-forge",
  "private": true,
  "version": "0.1.0",
  "description": "Tafy's Actor Forge — generatore di JSON importabili in Foundry VTT (dnd5e 5.3.3)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node test/smoke.mjs && node test/statblock.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
